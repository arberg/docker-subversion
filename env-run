#!/usr/bin/env bash

CURRENT_DIR="$( cd "$(dirname "${BASH_SOURCE[0]}")" ; pwd -P )"

. "$CURRENT_DIR/env"

DOCKER_NAME=subversion
HOST_PORT=3690

HOST_SVN_DIR=/mnt/disk1/svn
HOST_CONFIG_DATA_DIR=/mnt/user/docker/subversion


# For isolated network variant. Use a unique /24 per isolated docker.
NETWORK_ISOLATED_SUBNET="${NETWORK_ISOLATED_SUBNET:-172.16.1.0/24}"
NETWORK_ISOLATED_NAME="${NETWORK_ISOLATED_NAME:-subversion_isolated}"
